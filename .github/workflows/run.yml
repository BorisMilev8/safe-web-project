name: Run Safe Web MVP

on:
  push:
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
        
        
        - name: Run backend script
  run: |
    cd backend
    python3 safe_web_mvp.py
